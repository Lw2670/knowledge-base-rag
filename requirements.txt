# 个人知识库问答系统（RAG）依赖清单
# 安装：pip install -r requirements.txt
# 版本为作者实际验证可用的版本，建议保持一致以保证复现。

# ---- 核心 RAG 框架 ----
langchain==1.3.15
langchain-community==0.4.2
langchain-core==1.5.4
langchain-openai==1.5.0
langchain-text-splitters==1.1.2

# ---- 向量库（本地持久化）----
chromadb==1.5.9

# ---- 向量化（本地 embedding，无需联网）----
fastembed==0.8.0

# ---- 网页界面 ----
streamlit==1.61.1
rank_bm25==0.2.2
jieba==0.42.1
# ---- 模型下载（可选，scripts/download_model.py 用）----
modelscope
