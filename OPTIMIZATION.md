# RAG 问答系统 · 优化问题清单

> 系统梳理当前代码（rag.py / app.py）后的需求痛点，作为后续优化的依据。按「优先级 + 模块」组织。

## 一、大模型输出质量（核心 · P0）

| # | 问题 | 现状 | 优化方向 |
|---|---|---|---|
| 1.1 | 回答是"片段拼接" | 直接对检索片段做摘要，回答短、碎、浅 | 先让 LLM **整合提炼**检索结果，再深度组织成完整回答 |
| 1.2 | 缺"检索结果二次加工" | 4 条片段直接拼进 prompt | 增加"去重 → 合并 → 提炼要点"环节，去掉冗余噪声 |
| 1.3 | Prompt 过于简单 | 只一句"根据资料回答" | 要求结构化输出（分点/小标题）、先说结论再展开、控制深度 |
| 1.4 | 无相关性过滤 | `similarity_search` 返回 top-k，可能混入低相关片段 | 改用 `similarity_search_with_score` + 分数阈值过滤 |
| 1.5 | 多轮上下文浅 | 只拼最近 4 条历史 | 增加历史压缩/摘要，长对话不稀释检索质量 |

## 二、网页 UI 设计（P1）

| # | 问题 | 优化方向 |
|---|---|---|
| 2.1 | 布局单一、无视觉层次 | 增加欢迎页、分区、卡片式布局 |
| 2.2 | 配色默认（Streamlit 蓝） | 定制主题色（呼应知识库的绿色/teal 品牌） |
| 2.3 | 字体未优化 | 设置中文字体栈（微软雅黑/PingFang） |
| 2.4 | 缺欢迎页/空状态 | 无对话时展示引导语 + 示例问题 |
| 2.5 | 思考过程展示朴素 | st.status 做得更精致（图标、检索片段预览样式） |
| 2.6 | 引用来源朴素 | 卡片式展示，带来源类型/相关性标签 |
| 2.7 | 响应式/移动端 | 优化窄屏体验 |

## 三、代码结构 / 性能 / 可维护性（P1/P2）

| # | 问题 | 优化方向 |
|---|---|---|
| 3.1 | 每次调用新建 LLM 实例 | 模块级单例 / 缓存 |
| 3.2 | 职责混乱 | 拆 `indexer.py`（索引）/ `retriever.py`（检索）/ `generator.py`（生成） |
| 3.3 | 硬编码配置 | CHUNK_SIZE、KB_DIR、k 值等抽到统一配置 |
| 3.4 | 错误处理/日志不足 | 统一 try/except + 日志 |
| 3.5 | 检索器/生成器未抽象 | 抽接口，便于换向量库/LLM |
| 3.6 | 缺测试 | 补核心链路单测 |

## 四、其他发现的问题（跨领域）

- **BUG**：`app.py` 示例问题「我的三个 AI 项目分别是什么？」应为「**四个**」（与简历一致）
- **注释过时**：`rag.py` 顶部注释仍写「最小版」，与实际不符

## 优先级建议

- **第一批（P0）**：1.1~1.5 输出质量 + 修复"三个→四个"bug
- **第二批（P1）**：2.1~2.7 UI 美化 + 3.1~3.3 配置/单例/拆分
- **第三批（P2）**：3.4~3.6 抽象/测试
